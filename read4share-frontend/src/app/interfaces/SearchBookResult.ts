export interface SearchBookResult {
  title: string;
  isbn: string;
  author: string;
  publDate: string;
  publisher: string;
  language: string;
  coverUrl: string;
  category: string;
  avgRating: number;
}
